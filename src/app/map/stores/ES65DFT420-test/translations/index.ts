import en from './en.json'

/**
 * Translations which we are using for maps like directions speeches, and etc.,
 * NOTE: This translations are already integrated in the maps package. I just put this
 * in here to have a template if clients want to overried the default translations or
 * support language.
 */

export default {
  en,
}
